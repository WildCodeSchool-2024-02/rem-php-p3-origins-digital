{% extends "base.html.twig" %} 

{% block body %} 
<h1>Liste des Utilisateurs</h1>
        <ul>
            {% for user in users %}
                <li>
                    {{ user.firstName }} {{ user.lastName }} Pseudo {{user.pseudo}} 
                    - {{ user.email }} -  Date de souscription {{ user.subscriptionDate|date("d/m/y") }}
                    <th><a class="btn btn-primary" href="/admin/users/delete/{{ user.id }}" role="button" onclick="return confirmDelete()" >Delete</a></th>
                </li>
            {% else %}
                <li>Aucun utilisateur trouvé</li>
            {% endfor %}

            <div class="container btn_returndashboard">
        <a href="{{ path('dashboard') }}" class="btn btn-primary">Retour au Panneau Administrateur</a>
    </div>
        </ul> 
{% endblock %}