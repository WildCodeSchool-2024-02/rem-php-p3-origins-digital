{% extends 'base.html.twig' %}
{% block body %}
    <section class="quizzform">
        <h2>Questionnaire</h2>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Valider le quizz</button>
        {{ form_end(form) }}
    </section>
{% endblock %}
